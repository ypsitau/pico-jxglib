The file system supports RTC (Real Time Clock) modules to record the last modified time of files. When you create or update a file, the current time is reflected on the file's timestamp. `touch` command also works fine with this feature.

In this page, we will connect an RTC module to the Pico board by using shell commands.

{% include-markdown "../../device/rtc/index.md" start="<!-- mkdocs-start:skip-abstract -->" %}
