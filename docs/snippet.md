=== "C (SDK)"
    ```c
    // RP2350's code
    ```
=== "Python"
    ```python
    # MicroPython's code
    ```

$$
f_{cpu} = \frac{vco}{postdiv1 \times postdiv2}
$$

``` py linenums="1"
def bubble_sort(items):
    for i in range(len(items)):
        for j in range(len(items) - 1 - i):
            if items[j] > items[j + 1]:
                items[j], items[j + 1] = items[j + 1], items[j]
```

!!! note "note - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! abstract "abstract - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! info "info - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! tip "tip - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! success "success - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! question "question - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! warning "warning - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! failure "failure - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! danger "danger - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! bug "bug - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! example "example - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

!!! quote "quote - the core of the project"
    This project is not just a library, but a collection of tools that engineers can use as an immediate asset in the "field".

???+ note "confirm technical details (click to expand)"
    openThis is initially open (removing the + will make it closed).
    You can write more Markdown inside.

???- note "confirm technical details (click to expand)"
    This is initially closed (removing the + will make it open).
    You can write more Markdown inside.
